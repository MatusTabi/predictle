import Rank from './rank';

const Sidebar = () => {
    return (
        <section className="w-1/4 bg-primary-container rounded-lg p-4 flex flex-col gap-4">
            <Rank />
        </section>
    );
};

export default Sidebar;
